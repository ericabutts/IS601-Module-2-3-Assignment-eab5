{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Calculator Module",
            "type": "debugpy",
            "request": "launch",
            "module": "app.calculator.__init__",   // run the __init__.py as a module
            "cwd": "${workspaceFolder}",          // project root
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}
